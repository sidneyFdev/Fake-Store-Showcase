import { useAPI } from "@/contexts/useAPIContext"

const Contact = () => {

    const {apiData} = useAPI()

    return (
        <div>
            Contact
        </div>
    )
}

export default Contact